name: actions

description: actions

on:
  push:
    branches:
      - main

runs:
  using:
    jobs:
      test:
        runs-on: ubuntu-latest
        steps:
          - name: Repo auschecke
            uses: actions/checkout@v4